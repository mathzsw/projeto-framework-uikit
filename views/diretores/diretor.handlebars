<h1>Diretores</h1>

<a href="/diretores/cadastrar" class="uk-button uk-button-primary">
  Cadastrar novo diretor
</a>

<div class="uk-child-width-1-3@m uk-grid-small" uk-grid>
  {{#each diretores}}
    <div>
      <div class="uk-card uk-card-default uk-card-body">
        <h3>{{this.nome}}</h3>
        <p>{{this.nacionalidade}}</p>

        <a href="#modal-{{this.id}}" class="uk-button uk-button-text" uk-toggle>
          Ver informações
        </a>
        
        <a href="/diretores/{{this.id}}" class="uk-margin-small-left">
          Ver detalhes
        </a>

      <div id="modal-{{this.id}}" uk-modal>
        <div class="uk-modal-dialog uk-modal-body">
          <h2>{{this.nome}}</h2>
          <p>Ano de nascimento: {{this.anoNascimento}}</p>
          <p>Nacionalidade: {{this.nacionalidade}}</p>

          <button class="uk-modal-close-default" type="button" uk-close></button>
        </div>
      </div>
    </div>
  {{/each}}
</div>